package javaphase1;

public class library {
	public String name; //name of the library
	public String location; //the location of the library
	public int numWorkers; //the number of workers (should we remove this attribute?)
	public int nob; //number of books in the array
	public int noo; //number of orders in the array (should we change the name?)
	books inStore []; //array containing all the books in the library
	order ListOfOrders[]; //an array with all the orders that were ever made
	/* methods that will be added later(not complete)
	add book
	add order
	cancel order 
	cancel book
	*/

}
